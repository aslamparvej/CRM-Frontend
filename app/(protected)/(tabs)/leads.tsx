import {
  View,
  Text,
  TouchableOpacity,
} from "react-native";

import { router } from "expo-router";

export default function LeadsPage() {
  return (
    <View
      style={{
        flex: 1,
        padding: 20,
      }}
    >
    </View>
  );
}